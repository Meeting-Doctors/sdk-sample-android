<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff">

    <ImageView
        style="@style/MediQuoChatWallpaper"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/chatRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingTop="4dp"
            android:scrollbarStyle="outsideOverlay"
            android:scrollbars="vertical"
            android:visibility="visible" />

        <View
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:background="@color/meetingdoctors_dark_gray" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#eaeaea"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/attachment_button"
                android:layout_width="56dp"
                android:layout_height="match_parent"
                android:paddingStart="17dp"
                android:paddingEnd="17dp"
                app:srcCompat="@drawable/mediquo_attach_icon" />

            <EditText
                android:id="@+id/message_input"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                android:hint="@string/meetingdoctors_chat_write_message_hint"
                android:imeOptions="flagNoEnterAction"
                android:inputType="textMultiLine|textShortMessage"
                android:maxLines="5"
                android:minLines="1"
                android:paddingBottom="16dp"
                android:paddingTop="16dp"
                android:scrollHorizontally="false"
                android:singleLine="false"
                android:textColor="#FF191919"
                android:textColorHint="@color/meetingdoctors_dark_gray"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/send_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:clickable="true"
                android:padding="16dp"
                android:text="@string/meetingdoctors_chat_send"
                android:textColor="@color/meetingdoctors_black"
                android:textSize="16sp" />
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>